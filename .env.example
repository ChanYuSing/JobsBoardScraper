# Copy to .env and fill in your Supabase credentials.
# Get the URI from: Supabase → Project Settings → Database → Connection pooling → Transaction (port 6543)
DATABASE_URL=postgresql://postgres.<project-ref>:<your-password>@aws-1-<region>.pooler.supabase.com:6543/postgres
