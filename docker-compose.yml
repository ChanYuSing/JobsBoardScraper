services:
  jobboard:
    build: .
    ports:
      - "8001:8001"
    volumes:
      - jobboard_data:/app/data          # named volume = portable, fast native I/O
      - ./config.yaml:/app/config.yaml   # edit config without rebuilding

volumes:
  jobboard_data:
